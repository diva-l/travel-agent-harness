# Core runtime (mirrors pyproject.toml [project.dependencies])
fastapi>=0.116,<1
pydantic>=2.11,<3
uvicorn[standard]>=0.35,<1
urllib3>=2.0,<3
json_repair>=0.30,<1

# Tests: python -m unittest discover -s tests
# (test extra also needs httpx for fastapi.testclient)
httpx>=0.27,<1
